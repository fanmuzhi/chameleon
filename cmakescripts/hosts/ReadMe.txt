The names of sub-directories in this directory must correspond to the output
of the "uname" command on the given platform.  These names are used in build
scripts and Makefile files.

DO NOT change the names in this directory and make sure that new directory
names are added correctly.
